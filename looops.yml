- name: installing packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: packages names
    ansible.builtin.package:
    - name: {{ item }}
      state: latest
    loop:
    - nginx
    - zip
    - mysql
  