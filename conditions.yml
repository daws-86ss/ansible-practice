- name: conditions 
  hosts: local
  connection: local
  vars:
    NUMBER= 5
  tasks:
  - name: checking number is less than 10 or not
    ansible.builtin.debug:
      msg: "given number is {{ NUMBER }} is less than 10"
    when: NUMBER < 10
